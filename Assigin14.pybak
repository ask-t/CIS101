#Asuku,CIS 101, Oct4th, with Io Cheng
#step1 make character as many as size . numSpaces is starting as 0 
#step2 numSpace plus 1 and numchar subtract 2.
#step3 then make character and space again as many as size and numSpace
#step4 it is looping until numSpace is the number of size divied by 2



def pyramid(character,size):
  numChar = size-1
  numSpaces = 0
  while numSpaces  <= size/2 :
     print numSpaces*" "+numChar*character
     numSpaces =numSpaces+1
     numChar =numChar-2
    
